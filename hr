SELECT
  t.id AS task_id,
  t.name AS task_name,
  CASE
    WHEN AVG(r.score) <= 20 THEN 'Hard'
    WHEN AVG(r.score) > 20 AND AVG(r.score) <= 60 THEN 'Medium'
    ELSE 'Easy'
  END AS difficulty
FROM
  tasks t
JOIN
  reports r ON t.id = r.task_id
GROUP BY
  t.id, t.name
ORDER BY
  t.id;